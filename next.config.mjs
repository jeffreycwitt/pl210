/**
   * @type {import('next').NextConfig}
   */
 const nextConfig   = {
  images: {
    loader: 'akamai',
    path: './',
  },
  assetPrefix: '/pl201/',
  basePath: '/pl201',
};

export default   nextConfig;

